feedparser>=6.0
python-dotenv>=1.0
requests>=2.31
gradio>=4.0

# Slice 4 (AI fallback tagger). dghs-imgutils pins numpy<2, but numpy<2 has no
# prebuilt wheel for Python 3.14 -- pip falls back to an experimental
# MINGW-built numpy that segfaults on import. onnxruntime/opencv/imgutils all
# work fine with numpy 2.x despite the stale pin, so after installing this
# file, also run: pip install --upgrade numpy
dghs-imgutils
onnxruntime
